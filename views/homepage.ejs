<!DOCTYPE html>

<html lang="fr">

    <head>

        <meta charset="utf-8"/>
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta name="description" content="Ceci est la page d'accueil de l'utilisateur">

        <link rel="stylesheet" href="/public/style.css" media="all"/>

        <title>Home Page</title>

    </head>

    <body>
        <header>
            <h1>Vicky Airlines - Accueil</h1>
        </header>

        <main>

            <h2>Liste de vos destinations</h2>

                <div class="menu">

                    <form action="/agence" method="GET">
                        <button type="submit">Agences</button>
                    </form>

                    <form action="/delete_account" method="GET">
                        <button type="submit">Supprimer compte</button>
                    </form>

                    <form action="/userHome" method="GET">
                        <button type="submit">Log out</button>
                    </form>
                </div>
                
                <div class="block2">
                    <table>
                    
                        <thead>
                            <tr>
                                <td><strong>Pays</strong></td>
                                <td><strong>Ville</strong></td>
                                <td><strong>Jours</strong></td>
                            </tr>
                        </thead>

                        <tbody>
                            <% for (let i = 0 ; i < destination.length ; i++) {%>
                            <tr>
                                
                                <td><%= destination[i].country %></td>
                                
                                <td><%= destination[i].city %></td>
                                
                                <td><%= destination[i].days %></td>
                                
                                <td><form action="/destupdateform/<%= destination[i].iddestination %>" method="GET">
                                <button type="submit">Modifier</button>
                                </form></td>
                                
                                <td><form action="/destdeleteform/<%= destination[i].iddestination %>" method="GET">
                                <button type="submit">Supprimer</button>
                                </form></td>
                                
                            </tr>
                            <% } %>
                        </tbody>
                    
                    </table>

                        <form action="/destaddform" method="GET">
                            <button type="submit">Ajouter destination</button>
                        </form>
                </div>
            
        </main>

        <footer>

        </footer>


    </body>
</html>