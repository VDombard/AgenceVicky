<!DOCTYPE html>
<html>
    <head>

        <meta charset="utf-8"/>
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta name="agenceListe" content="ceci est la liste des agences de voyages du l'utilisateur">

        <link rel="stylesheet" href="/public/style.css" media="all"/>

        <title>Agences</title>

    </head>

    <body> 

        <h1>Liste des agences</h1>

        <form action = "/homepage" method="GET">
            <button type="submit">Page d'accueil</button>

        </form>
        <p>* L'agence s'affiche seulement si vous avez une destination liée à cette agence de voyage</p>
        <div>
            <table>
                <% for(let i = 0; i < agence.length; i++) { %>
                <tr>
                    <td>
                        <form action="/agenceupdateform/<%= agence[i].idagence %>" method="GET">
                        <button type="submit">Modifier</button>
                        </form>
                        
                        <form action="/agenceaddform" method="GET">
                        <button type="submit">Ajouter</button>
                        </form>
                    </td>

                    <td><%= agence[i].idagence %> </td>
                    <td><%= agence[i].nomagence %> </td> 

                </tr>
                <% } %>
            </table>
        </div>

    </body>

    <footer></footer>
</html>